import RGBAColor from '../source/color/RGBAColor.js';
import RGBColor from '../source/color/RGBColor.js';

const rgb = new RGBColor({
    r: 255,
});

const rgba = new RGBAColor({
    r: 255,
});

console.log(`${rgb}`);
console.log(`${rgba}`);